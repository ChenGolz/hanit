
window.PETCONNECT_TRANSLATIONS = {"he": {"brand": "Israel PetConnect AI", "home": "בית", "found": "מצאתי חיה", "lost": "איבדתי חיה", "matches": "התאמות", "volunteer": "מתנדבים", "shelter": "מקלטים", "heroBadge": "🐾 איתור חיות מחמד אבודות בישראל", "heroTitle": "הדרך הכי מהירה לחבר בין “איבדתי” ל“מצאתי”", "heroText": "גרסה סטטית שעובדת על GitHub Pages. הדיווח מהנייד, בחירת מיקום, ריבוי שפות וקריאות API לשרת נשמרו — אבל ה-AI והמסד עדיין חייבים לרוץ בשרת חיצוני.", "oneTap": "מצאתי חיה משוטטת", "reportLost": "לדווח על חיה אבודה", "checkMatches": "בדוק התאמות", "joinVolunteer": "הצטרפו כמתנדבים", "feature1": "דיווח מהיר מהמובייל", "feature1t": "פתיחה מהירה של מצלמה, העלאת תמונה או וידאו קצר, ושימוש במיקום הנוכחי.", "feature2": "GitHub Pages-ready", "feature2t": "עובד כאתר סטטי אמיתי — בלי להיתקע על README או על build של Next.", "feature3": "API חיצוני", "feature3t": "האתר קורא ל-FastAPI שלכם דרך URL שמוגדר ב-config.js.", "feature4": "עברית / عربي / English", "feature4t": "החלפת שפה מיידית עם כיוון RTL/LTR תקין.", "feature5": "דפי ערים", "feature5t": "דפי נחיתה מוכנים לערים מרכזיות עם קישורים לדיווחים רלוונטיים.", "feature6": "PWA בסיסי", "feature6t": "manifest ו-service worker בסיסיים לשיפור חוויית מובייל.", "apiWarning": "כדי לשלוח טפסים באמת, ערכו את הקובץ config.js ושימו בו את כתובת השרת, למשל https://api.petconnect.co.il", "stats1": "מובייל-פירסט", "stats2": "3 שפות", "stats3": "GitHub Pages", "cityTitle": "דפי עיר", "worksNow": "הגרסה הזו תעלה על GitHub Pages מיד", "worksNowText": "פשוט שימו את הקבצים בריפו ובחרו Pages → Deploy from a branch → main / root.", "foundTitle": "מצאתי חיה — דיווח מהיר", "lostTitle": "דיווח על חיה אבודה", "matchesTitle": "דיווחים פעילים", "volunteerTitle": "מתנדב שכונתי", "shelterTitle": "סריקת קליטת מקלט", "name": "שם החיה", "animalType": "סוג חיה", "breed": "גזע", "color": "צבע", "collar": "תיאור קולר", "markings": "סימנים מזהים", "city": "עיר", "neighborhood": "שכונה", "location": "מיקום", "notes": "הערות", "contactName": "שם איש קשר", "phone": "טלפון", "email": "אימייל", "image": "תמונה", "video": "וידאו קצר", "nosePhoto": "תמונת אף מקרוב", "microchip": "מספר שבב", "verificationPrompt": "שאלת אימות פרטית", "verificationAnswer": "תשובה לשאלת האימות", "privateMarkerPrompt": "סימן פיזי פרטי", "finderName": "שם המוצא", "useLocation": "השתמש במיקום שלי", "save": "שמור דיווח", "saveVolunteer": "שמור מתנדב", "runShelter": "בצע סריקה", "apiBase": "כתובת שרת", "reportSaved": "הדיווח נשמר.", "volunteerSaved": "המתנדב נשמר.", "shelterSaved": "סריקת המקלט נשמרה.", "loading": "טוען...", "none": "אין עדיין נתונים.", "municipalDraft": "טיוטת 106", "possibleMatches": "התאמות אפשריות", "heatmap": "מפת sightings", "footer": "אתר סטטי ל-GitHub Pages • backend חיצוני נדרש ל-AI, DB, SMS והתראות", "sourcePlatform": "פלטפורמת מקור", "radiusKm": "רדיוס ק״מ", "shelterName": "שם המקלט / הרשות", "bestFrame": "וידאו נתמך — בחירת best frame נעשית בשרת", "privateMarkerConfirm": "אפשר לאשר את הסימן הפיזי שהבעלים תיארו"}, "en": {"brand": "Israel PetConnect AI", "home": "Home", "found": "Found pet", "lost": "Lost pet", "matches": "Matches", "volunteer": "Volunteer", "shelter": "Shelter", "heroBadge": "🐾 Lost pet recovery across Israel", "heroTitle": "A GitHub Pages version that actually works", "heroText": "This static build renders correctly on GitHub Pages. Mobile reporting, maps, multilingual UI, and API calls remain — but AI, database, and notifications still need a separate backend.", "oneTap": "I found a stray animal", "reportLost": "Report a missing pet", "checkMatches": "View matches", "joinVolunteer": "Join volunteers", "feature1": "Fast mobile reporting", "feature1t": "Open the camera quickly, upload a photo or short video, and use your current location.", "feature2": "GitHub Pages-ready", "feature2t": "A real static site — no README landing page and no Next.js deployment confusion.", "feature3": "External API", "feature3t": "The frontend calls your FastAPI backend through a URL set in config.js.", "feature4": "Hebrew / Arabic / English", "feature4t": "Instant language switching with proper RTL/LTR support.", "feature5": "City landing pages", "feature5t": "Ready-made city pages for major locations in Israel.", "feature6": "Basic PWA", "feature6t": "Includes a manifest and lightweight service worker for a better mobile feel.", "apiWarning": "To make form submissions work, edit config.js and set your backend URL, for example https://api.petconnect.co.il", "stats1": "Mobile-first", "stats2": "3 languages", "stats3": "GitHub Pages", "cityTitle": "City pages", "worksNow": "This version will open on GitHub Pages immediately", "worksNowText": "Just upload these files to your repo and set Pages → Deploy from a branch → main / root.", "foundTitle": "I found a pet — quick report", "lostTitle": "Report a missing pet", "matchesTitle": "Active reports", "volunteerTitle": "Neighborhood volunteer", "shelterTitle": "Shelter intake scan", "name": "Pet name", "animalType": "Animal type", "breed": "Breed", "color": "Color", "collar": "Collar description", "markings": "Unique markings", "city": "City", "neighborhood": "Neighborhood", "location": "Location", "notes": "Notes", "contactName": "Contact name", "phone": "Phone", "email": "Email", "image": "Photo", "video": "Short video", "nosePhoto": "Nose-up photo", "microchip": "Microchip number", "verificationPrompt": "Private verification question", "verificationAnswer": "Verification answer", "privateMarkerPrompt": "Private physical marker", "finderName": "Finder name", "useLocation": "Use my location", "save": "Save report", "saveVolunteer": "Save volunteer", "runShelter": "Run scan", "apiBase": "API base URL", "reportSaved": "Report saved.", "volunteerSaved": "Volunteer saved.", "shelterSaved": "Shelter scan saved.", "loading": "Loading...", "none": "No data yet.", "municipalDraft": "106 draft", "possibleMatches": "Possible matches", "heatmap": "Sighting heat map", "footer": "Static GitHub Pages frontend • external backend still required for AI, DB, SMS, and alerts", "sourcePlatform": "Source platform", "radiusKm": "Radius km", "shelterName": "Shelter / authority name", "bestFrame": "Video is supported — best frame selection happens on the backend", "privateMarkerConfirm": "I can confirm the physical marker described by the owner"}, "ar": {"brand": "Israel PetConnect AI", "home": "الرئيسية", "found": "وجدت حيوانًا", "lost": "فقدت حيوانًا", "matches": "المطابقات", "volunteer": "متطوع", "shelter": "ملجأ", "heroBadge": "🐾 العثور على الحيوانات المفقودة في إسرائيل", "heroTitle": "نسخة تعمل فعلًا على GitHub Pages", "heroText": "هذه نسخة ثابتة تُعرض بشكل صحيح على GitHub Pages. ما زالت الواجهة متعددة اللغات وتدعم التقاطًا من الهاتف واستدعاءات API، لكن الذكاء الاصطناعي وقاعدة البيانات والتنبيهات تحتاج خادمًا منفصلًا.", "oneTap": "وجدت حيوانًا شاردًا", "reportLost": "الإبلاغ عن حيوان مفقود", "checkMatches": "عرض المطابقات", "joinVolunteer": "انضم للمتطوعين", "feature1": "تبليغ سريع من الهاتف", "feature1t": "فتح الكاميرا بسرعة، رفع صورة أو فيديو قصير، واستخدام الموقع الحالي.", "feature2": "جاهز لـ GitHub Pages", "feature2t": "موقع ثابت حقيقي بدون الوقوع في صفحة README أو تعقيدات نشر Next.", "feature3": "واجهة API خارجية", "feature3t": "الواجهة تستدعي خادم FastAPI عبر رابط يتم ضبطه في config.js.", "feature4": "עברית / عربي / English", "feature4t": "تبديل فوري للغة مع دعم RTL وLTR.", "feature5": "صفحات مدن", "feature5t": "صفحات جاهزة لمدن رئيسية في إسرائيل.", "feature6": "PWA أساسي", "feature6t": "manifest وservice worker خفيف لتجربة جوال أفضل.", "apiWarning": "لكي تعمل النماذج فعليًا، عدّل config.js وضع رابط الخادم، مثل https://api.petconnect.co.il", "stats1": "أولًا للهاتف", "stats2": "3 لغات", "stats3": "GitHub Pages", "cityTitle": "صفحات المدن", "worksNow": "هذه النسخة ستفتح على GitHub Pages فورًا", "worksNowText": "ارفع هذه الملفات إلى المستودع واضبط Pages → Deploy from a branch → main / root.", "foundTitle": "وجدت حيوانًا — بلاغ سريع", "lostTitle": "الإبلاغ عن حيوان مفقود", "matchesTitle": "البلاغات النشطة", "volunteerTitle": "متطوع حيّ", "shelterTitle": "فحص استقبال الملجأ", "name": "اسم الحيوان", "animalType": "نوع الحيوان", "breed": "السلالة", "color": "اللون", "collar": "وصف الطوق", "markings": "علامات مميزة", "city": "المدينة", "neighborhood": "الحي", "location": "الموقع", "notes": "ملاحظات", "contactName": "اسم جهة الاتصال", "phone": "الهاتف", "email": "البريد الإلكتروني", "image": "الصورة", "video": "فيديو قصير", "nosePhoto": "صورة مقربة للأنف", "microchip": "رقم الشريحة", "verificationPrompt": "سؤال تحقق خاص", "verificationAnswer": "إجابة التحقق", "privateMarkerPrompt": "علامة جسدية خاصة", "finderName": "اسم الواجد", "useLocation": "استخدم موقعي", "save": "احفظ البلاغ", "saveVolunteer": "احفظ المتطوع", "runShelter": "شغّل الفحص", "apiBase": "رابط الـ API", "reportSaved": "تم حفظ البلاغ.", "volunteerSaved": "تم حفظ المتطوع.", "shelterSaved": "تم حفظ فحص الملجأ.", "loading": "جارٍ التحميل...", "none": "لا توجد بيانات بعد.", "municipalDraft": "مسودة 106", "possibleMatches": "مطابقات محتملة", "heatmap": "خريطة المشاهدات", "footer": "واجهة ثابتة لـ GitHub Pages • ما زال الخادم الخارجي مطلوبًا للذكاء الاصطناعي وقاعدة البيانات والرسائل", "sourcePlatform": "منصة المصدر", "radiusKm": "نطاق كم", "shelterName": "اسم الملجأ / الجهة", "bestFrame": "الفيديو مدعوم — اختيار أفضل إطار يتم على الخادم", "privateMarkerConfirm": "يمكنني تأكيد العلامة الجسدية التي وصفها المالك"}};
window.PETCONNECT_CITIES = [{"slug": "tel-aviv", "he": "תל אביב", "ar": "تل أبيب", "en": "Tel Aviv"}, {"slug": "jerusalem", "he": "ירושלים", "ar": "القدس", "en": "Jerusalem"}, {"slug": "haifa", "he": "חיפה", "ar": "حيفا", "en": "Haifa"}, {"slug": "beer-sheva", "he": "באר שבע", "ar": "بئر السبع", "en": "Beer Sheva"}, {"slug": "netanya", "he": "נתניה", "ar": "نتانيا", "en": "Netanya"}, {"slug": "ashdod", "he": "אשדוד", "ar": "أشدود", "en": "Ashdod"}];
(function() {
  const translations = window.PETCONNECT_TRANSLATIONS;
  const cities = window.PETCONNECT_CITIES;
  const localeMeta = { he: {label:'עברית', dir:'rtl'}, ar: {label:'العربية', dir:'rtl'}, en: {label:'English', dir:'ltr'} };
  const state = { locale: localStorage.getItem('petconnect_locale') || 'he' };
  const cfg = window.PETCONNECT_CONFIG || { API_BASE_URL: '' };

  function t(key) { return (translations[state.locale] && translations[state.locale][key]) || translations.en[key] || key; }
  function escapeHtml(s) { return String(s ?? '').replace(/[&<>"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c])); }
  function mask(v) { if (!v) return ''; return v; }
  function apiBase() { return (window.PETCONNECT_CONFIG && window.PETCONNECT_CONFIG.API_BASE_URL || '').replace(/\/$/, ''); }
  function apiReady() { return !!apiBase(); }
  function setNotice(el, type, msg) { if (!el) return; el.className = 'notice ' + type; el.textContent = msg; el.hidden = false; }
  function clearNotice(el) { if (!el) return; el.hidden = true; el.textContent=''; el.className='notice'; }
  function applyI18n(root=document) {
    document.documentElement.lang = state.locale;
    document.documentElement.dir = localeMeta[state.locale].dir;
    root.querySelectorAll('[data-i18n]').forEach(el => { el.textContent = t(el.dataset.i18n); });
    root.querySelectorAll('[data-i18n-placeholder]').forEach(el => { el.placeholder = t(el.dataset.i18nPlaceholder); });
    root.querySelectorAll('[data-i18n-html]').forEach(el => { el.innerHTML = t(el.dataset.i18nHtml); });
    const select = root.querySelector('#language-select'); if (select) select.value = state.locale;
    const brand = root.querySelector('#brand-text'); if (brand) brand.textContent = t('brand');
  }
  function initLang() {
    document.querySelectorAll('#language-select').forEach(sel => sel.addEventListener('change', e => { state.locale = e.target.value; localStorage.setItem('petconnect_locale', state.locale); applyI18n(document); renderCityGrid(); renderCityPageTitle(); }));
    applyI18n(document);
  }
  function renderCityGrid() {
    document.querySelectorAll('[data-city-grid]').forEach(wrap => {
      wrap.innerHTML = cities.map(c => `<a class="city-link" href="${relativePrefix()}${c.slug}/">${c[state.locale]}</a>`).join('');
    });
  }
  function renderCityPageTitle() {
    const slug = document.body.dataset.citySlug;
    if (!slug) return;
    const city = cities.find(c => c.slug === slug); if (!city) return;
    const el = document.getElementById('city-page-title'); if (el) el.textContent = city[state.locale];
    const sub = document.getElementById('city-page-subtitle'); if (sub) sub.textContent = t('matchesTitle') + ' — ' + city[state.locale];
  }
  function relativePrefix() {
    const path = location.pathname.replace(/index\.html$/, '');
    const depth = path.split('/').filter(Boolean).length - 1;
    return depth <= 0 ? '' : '../'.repeat(depth);
  }
  function initConfigEcho() {
    document.querySelectorAll('[data-api-url]').forEach(el => el.textContent = apiBase() || 'config.js');
    if (!apiReady()) document.querySelectorAll('[data-api-warning]').forEach(el => { el.hidden = false; el.textContent = t('apiWarning'); });
  }
  function initGeolocate() {
    document.querySelectorAll('[data-use-location]').forEach(btn => btn.addEventListener('click', () => {
      const form = btn.closest('form');
      if (!navigator.geolocation) return;
      navigator.geolocation.getCurrentPosition(pos => {
        form.querySelector('[name="latitude"]').value = pos.coords.latitude.toFixed(6);
        form.querySelector('[name="longitude"]').value = pos.coords.longitude.toFixed(6);
        if (form._leafletMap) {
          form._leafletMap.setView([pos.coords.latitude, pos.coords.longitude], 14);
          if (form._marker) form._leafletMap.removeLayer(form._marker);
          form._marker = L.marker([pos.coords.latitude, pos.coords.longitude]).addTo(form._leafletMap);
        }
      });
    }));
  }
  function initMaps() {
    if (typeof L === 'undefined') return;
    document.querySelectorAll('[data-map]').forEach(el => {
      const form = el.closest('form');
      const latInput = form.querySelector('[name="latitude"]');
      const lngInput = form.querySelector('[name="longitude"]');
      const map = L.map(el).setView([31.7683, 35.2137], 7);
      form._leafletMap = map;
      L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', { maxZoom: 19, attribution: '&copy; OpenStreetMap' }).addTo(map);
      map.on('click', e => {
        latInput.value = e.latlng.lat.toFixed(6);
        lngInput.value = e.latlng.lng.toFixed(6);
        if (form._marker) map.removeLayer(form._marker);
        form._marker = L.marker(e.latlng).addTo(map);
      });
      setTimeout(() => map.invalidateSize(), 200);
    });
    const mm = document.getElementById('matches-map');
    if (mm) {
      const map = L.map(mm).setView([31.7683, 35.2137], 7);
      window._matchesMap = map;
      L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', { maxZoom: 19, attribution: '&copy; OpenStreetMap' }).addTo(map);
      setTimeout(() => map.invalidateSize(), 200);
    }
  }
  function fdToObject(fd) { const obj={}; for (const [k,v] of fd.entries()) if (!(v instanceof File) || v.name) obj[k]=v; return obj; }
  async function postMultipart(form, endpoint, successMsg) {
    const note = form.querySelector('.notice') || document.getElementById('page-notice');
    clearNotice(note);
    if (!apiReady()) return setNotice(note, 'warn', t('apiWarning'));
    const fd = new FormData(form);
    try {
      const res = await fetch(apiBase() + endpoint, { method:'POST', body: fd });
      const data = await res.json();
      if (!res.ok) throw new Error(data.detail || 'Request failed');
      setNotice(note, 'ok', successMsg);
      renderApiResult(form, data);
    } catch (err) { setNotice(note, 'err', err.message || 'Request failed'); }
  }
  async function postJson(form, endpoint, successMsg) {
    const note = form.querySelector('.notice') || document.getElementById('page-notice');
    clearNotice(note);
    if (!apiReady()) return setNotice(note, 'warn', t('apiWarning'));
    const fd = new FormData(form); const payload={}; fd.forEach((v,k)=>payload[k]=v);
    if (payload.channels) payload.channels = ['sms'];
    ['latitude','longitude','radius_km'].forEach(k => { if (payload[k] !== '' && payload[k] != null) payload[k] = Number(payload[k]); else if (k !== 'radius_km') payload[k] = null; });
    try {
      const res = await fetch(apiBase() + endpoint, { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload) });
      const data = await res.json();
      if (!res.ok) throw new Error(data.detail || 'Request failed');
      setNotice(note, 'ok', successMsg);
      renderApiResult(form, data);
    } catch (err) { setNotice(note, 'err', err.message || 'Request failed'); }
  }
  function renderApiResult(form, data) {
    const out = form.parentElement.querySelector('[data-result]');
    if (!out) return;
    const parts = [];
    if (data.best_frame) parts.push(`<div class="result-card"><strong>${t('bestFrame')}</strong><div class="small">${escapeHtml(JSON.stringify(data.best_frame))}</div></div>`);
    if (data.municipal_draft) parts.push(`<div class="result-card"><strong>${t('municipalDraft')}</strong><div class="small">${escapeHtml(data.municipal_draft.subject || '')}</div><div class="small">${escapeHtml(data.municipal_draft.body || '')}</div></div>`);
    if (Array.isArray(data.matches) && data.matches.length) parts.push(`<div class="result-card"><strong>${t('possibleMatches')}</strong><div class="list">${data.matches.map(m => `<div><strong>${escapeHtml(m.name || 'Pet')}</strong> · ${escapeHtml(m.city || '')} · score ${escapeHtml(m.score || '')}</div>`).join('')}</div></div>`);
    else if (Array.isArray(data.matches)) parts.push(`<div class="result-card">${t('none')}</div>`);
    out.innerHTML = parts.join('');
  }
  function initForms() {
    const lost = document.getElementById('lost-form'); if (lost) lost.addEventListener('submit', e => { e.preventDefault(); postMultipart(lost, '/api/lost-pets', t('reportSaved')); });
    const found = document.getElementById('found-form'); if (found) found.addEventListener('submit', e => { e.preventDefault(); postMultipart(found, '/api/found-reports', t('reportSaved')); });
    const shelter = document.getElementById('shelter-form'); if (shelter) shelter.addEventListener('submit', e => { e.preventDefault(); postMultipart(shelter, '/api/shelter/intake-scan', t('shelterSaved')); });
    const vol = document.getElementById('volunteer-form'); if (vol) vol.addEventListener('submit', e => { e.preventDefault(); postJson(vol, '/api/volunteers', t('volunteerSaved')); });
  }
  async function loadMatches() {
    const list = document.getElementById('matches-list');
    if (!list) return;
    if (!apiReady()) { list.innerHTML = `<div class="empty">${escapeHtml(t('apiWarning'))}</div>`; return; }
    list.innerHTML = `<div class="empty">${escapeHtml(t('loading'))}</div>`;
    const params = new URLSearchParams(location.search);
    const city = document.body.dataset.citySlug || params.get('city') || '';
    const q = city ? '?city=' + encodeURIComponent(city) : '';
    try {
      const res = await fetch(apiBase() + '/api/lost-pets' + q);
      const data = await res.json();
      if (!res.ok) throw new Error(data.detail || 'Failed');
      if (!Array.isArray(data) || !data.length) { list.innerHTML = `<div class="empty">${escapeHtml(t('none'))}</div>`; return; }
      list.innerHTML = data.map(item => `
        <div class="result-card">
          <div class="inline" style="justify-content:space-between;align-items:flex-start;">
            <div>
              <div class="status-pill">${escapeHtml(item.status || 'active')}</div>
              <h3 style="margin:10px 0 6px;">${escapeHtml(item.name || 'Pet')}</h3>
              <div class="small">${escapeHtml(item.animal_type || '')} · ${escapeHtml(item.breed || '')} · ${escapeHtml(item.city || '')}</div>
              <div class="small">${escapeHtml(item.color || '')} · ${escapeHtml(item.unique_markings || '')}</div>
            </div>
            ${item.image_url ? `<img src="${apiBase() + item.image_url}" alt="pet" style="width:120px;height:120px;object-fit:cover;border-radius:16px;border:1px solid var(--line);">` : ''}
          </div>
          <div class="small" style="margin-top:12px;">${escapeHtml(item.last_seen_location || '')}</div>
          <div class="actions" style="margin-top:12px;">
            <a class="button" href="${relativePrefix()}matches/?city=${encodeURIComponent(item.city_slug || item.city || '')}">${escapeHtml(t('heatmap'))}</a>
          </div>
        </div>`).join('');
      if (window._matchesMap && typeof L !== 'undefined') {
        data.forEach(item => { if (item.latitude && item.longitude) L.marker([item.latitude, item.longitude]).addTo(window._matchesMap).bindPopup(`<strong>${escapeHtml(item.name || 'Pet')}</strong><br>${escapeHtml(item.city || '')}`); });
      }
    } catch (err) { list.innerHTML = `<div class="empty">${escapeHtml(err.message || 'Failed')}<\/div>`; }
  }
  function initPwa() {
    if ('serviceWorker' in navigator) navigator.serviceWorker.register(relativePrefix() + 'sw.js').catch(() => {});
  }
  document.addEventListener('DOMContentLoaded', () => { initLang(); renderCityGrid(); renderCityPageTitle(); initConfigEcho(); initGeolocate(); initMaps(); initForms(); loadMatches(); initPwa(); });
})();
